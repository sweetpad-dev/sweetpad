{
  "name": "@sweetpad/lib",
  "version": "0.1.1",
  "private": true,
  "description": "Internal — do not use. Rust resolver for Xcode build settings, exposed to Node via N-API for the SweetPad VS Code extension.",
  "license": "MIT OR Apache-2.0",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "sweetpad-lib",
    "targets": [
      "aarch64-apple-darwin",
      "x86_64-apple-darwin",
      "universal-apple-darwin"
    ]
  },
  "scripts": {
    "build": "napi build --platform --release --features node",
    "build:debug": "napi build --platform --features node",
    "build:universal": "napi build --platform --release --features node --target aarch64-apple-darwin && napi build --platform --release --features node --target x86_64-apple-darwin && napi universalize",
    "universalize": "napi universalize"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.0.0"
  }
}
